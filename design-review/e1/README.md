# Portfolio E1 visual review

Open `index.html` directly or serve this folder on loopback. The sample is offline, has no analytics or API calls and does not import the production entry. Agenc-IA is the primary case; ASK Painting and Colegio Santa Rosa de Lima appear as secondary client work.

Run `node check.mjs` for the smallest structural and asset-integrity check. The portrait is an identity-preserving, AI-assisted studio edit derived from Daniel's user-supplied photograph.
